using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CS_Randomizer_Islands : MonoBehaviour {

    /*public List<GameObject> IslandsList = new List<GameObject>();
    /* public GameObject I1;
     public GameObject I2;
     public GameObject I3;
 
    public int Icount = 0;
    public int Ilimit = 4;
    public bool allowSpawn = true;
    
    void Start()
    {
  /*      IslandsList.Add(I1);
        IslandsList.Add(I2);
        IslandsList.Add(I3);
        //SpawnIslands();
    }
	
	
    void Update()
    {
        
        /*if(allowSpawn)
        {
            SpawnIslands();
        }

        if (Icount < Ilimit)
        {
            SpawnIslands();
        }
        else
        {
            allowSpawn = false;
        }


    }
    
    public void SpawnIslands()
    {
        if (allowSpawn == true)
        {
            Vector3 sP = Camera.main.ScreenToWorldPoint(new Vector3(Random.Range(0, Screen.width), Random.Range(0, Screen.height), Camera.main.farClipPlane / 2));

            int rP = UnityEngine.Random.Range(0, IslandsList.Count - 1);
            Instantiate(IslandsList[rP], sP, Quaternion.identity);
            Icount++;
        }
    }*/
}
